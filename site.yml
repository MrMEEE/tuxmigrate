---
# Applied to localhost by the RPM post-install script.
# Managed by tuxmigrate - do not edit manually.
- name: Apply tuxmigrate configuration changes
  hosts: localhost
  connection: local
  become: true
  gather_facts: true

  roles:
    - role: /usr/share/tuxmigrate/role
